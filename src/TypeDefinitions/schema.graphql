type Query {
    hello(name: String): String!
}

type Mutation {
    register(publicAddress: String!, email: String!, firstName: String!, lastName: String!, isTrainer: Boolean!, specialty: String): Boolean

    signIn(publicAddress: String!, signature: String!): Boolean
}